services:
  postgres:
    image: postgres:16
    container_name: cars-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: carsdb
      POSTGRES_USER: carsuser
      POSTGRES_PASSWORD: carspass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U carsuser -d carsdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: cars-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]

volumes:
  postgres_data: